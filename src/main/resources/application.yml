spring:
  datasource:
    url: jdbc:mysql://localhost:3306/newron_db?createDatabaseIfNotExist = true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true